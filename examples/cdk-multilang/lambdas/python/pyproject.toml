[build-system]
requires = ["setuptools>=83.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tabletheory-cdk-multilang-python-demo"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
  "cryptography==49.0.0",
  "PyYAML==6.0.3",
]

[tool.setuptools]
py-modules = []
