# Python Contract Runner (minimal)

This runner validates shared, cross-language contract fixtures for the TableTheory Python implementation.

## Run

From the repo root:

```bash
uv --directory py run pytest -q ../contract-tests/runners/py
```

